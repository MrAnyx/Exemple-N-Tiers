{% extends "template/layout.twig" %}
{% block title %}Article | {{ article.titre }}{% endblock %}
{% block body %}

<a href="{{ generate("articleByUser", {idUser: article.idUser}) }}" class="btn btn-primary mb-4"> &lt Retour</a>

<h1>{{ article.titre }}</h1>

<span class="text-muted">{{ article.date }}</span>

<p class="mt-2">{{ article.contenu }}</p>

{% endblock %}